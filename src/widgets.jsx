export function Tgl({value,onChange,label,help,ariaLabel}){return(<div className="tw"><button type="button" className={`tg ${value?"on":""}`} onClick={()=>onChange(!value)} role="switch" aria-checked={!!value} aria-label={ariaLabel||label||help||"Toggle"}><div className="dk"/></button>{label&&<span className="tl">{label}</span>}</div>)}
export function IB({unit,value,onChange,step="0.1",warm=false,help,ariaLabel}){const lbl=ariaLabel||(unit?`Value in ${unit}`:"Numeric value");return(<div className="ib">{unit&&<span className="iu" aria-hidden="true">{unit}</span>}<input className={`iv${warm?" warm":""}`} type="number" value={value} step={step} onChange={e=>onChange(parseFloat(e.target.value)||0)} aria-label={lbl}/></div>)}
